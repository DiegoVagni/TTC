Il progetto è stato fatto cercando di mostrare un po' più le mie capacità. 
L'unica libreria oltre a react che ho utilizzato, come scritto nel package.json, è react-transition-group per le animazioni.

Funzionamento:
npm install
npm start